<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Suppress warning, just using queries tag is not enough for our all, we need access to all
    packages to allow the user to select apps for split tunneling -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- https://developer.android.com/guide/components/fg-service-types#system-exempted -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Added by work manager, it is not required for our use case. -->
    <uses-permission
        android:name="android.permission.WAKE_LOCK"
        tools:node="remove" />
    <uses-feature android:name="android.hardware.touchscreen"
                  android:required="false" />
    <uses-feature android:name="android.hardware.faketouch"
                  android:required="false" />
    <uses-feature android:name="android.hardware.screen.portrait"
                  android:required="false" />
    <uses-feature android:name="android.hardware.screen.landscape"
                  android:required="false" />
    <uses-feature android:name="android.software.leanback"
                  android:required="false" />
    <uses-feature android:glEsVersion="0x00020000"
                  android:required="false" />

    <application android:name=".app.WarrenApplication"
                 android:allowBackup="false"
                 tools:targetApi="33"
                 android:memtagMode="async"
                 android:fullBackupContent="@xml/full_backup_content"
                 android:dataExtractionRules="@xml/data_extraction_rules"
                 android:icon="@mipmap/ic_launcher"
                 android:label="@string/app_name"
                 android:roundIcon="@mipmap/ic_launcher"
                 android:theme="@style/Theme.App.Starting"
                 android:localeConfig="@xml/locales_config"
                 android:supportsRtl="true"
                 tools:ignore="CredManMissingDal,CredentialDependency,GoogleAppIndexingWarning">

        <!--
            This disables the Firebase logging that is added by the billing library.
        -->
        <service
                android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService"
                tools:node="remove"
                tools:ignore="MissingClass" />

        <profileable
                android:enabled="true"
                android:shell="true" />

        <!--
            MainActivity
            Must be exported in order to be launchable.
            Launch mode should singleInstance to avoid this vulnerability;
            https://developer.android.com/privacy-and-security/risks/strandhogg
            This can be disregarded when the minimum supported version is 29 or higher
            since after that it has been patched on a OS level.
        -->
        <activity
            android:name="com.warrenbrowse.vpn.app.MainActivity"
            android:enableOnBackInvokedCallback="true"
            android:launchMode="singleInstance"
            android:screenOrientation="fullUser"
            android:windowSoftInputMode="adjustResize"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:banner="@mipmap/ic_banner"
            tools:ignore="DiscouragedApi,UnusedAttribute">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.warrenbrowse.vpn.request_vpn_permission" />
            </intent-filter>
            <!--
                Community-forum wallet login (doc 55): the connect provider's
                approval page invokes `warren://forum-login?sid=..&host=..`; the
                OS hands it here (MainActivity.handleIntent validates + prompts).
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="${warrenDeepLinkScheme}" android:host="forum-login" />
            </intent-filter>
        </activity>
        <!--
            foregroundServiceType="systemExempted" is required in Android 14+
            https://developer.android.com/guide/components/fg-service-types#system-exempted
            -->
        <service android:name="com.warrenbrowse.vpn.app.service.WarrenVpnService"
                 android:exported="false"
                 android:foregroundServiceType="systemExempted"
                 android:permission="android.permission.BIND_VPN_SERVICE"
                 android:stopWithTask="false"
                 tools:ignore="ForegroundServicePermission">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.warrenbrowse.vpn.connect_action" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.warrenbrowse.vpn.reconnect_action" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.warrenbrowse.vpn.disconnect_action" />
            </intent-filter>
        </service>
        <!--
            WarrenTileService
            Tile services must be exported and protected by the bind tile permission
            (android.permission.BIND_QUICK_SETTINGS_TILE).
        -->
        <service android:name="com.warrenbrowse.vpn.app.tile.WarrenTileService"
                 android:exported="true"
                 android:icon="@drawable/small_logo_black"
                 android:label="@string/toggle_vpn"
                 android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <meta-data android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
        <receiver android:name=".receiver.LocaleChangedBroadcastReceiver"
                  android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.LOCALE_CHANGED" />
            </intent-filter>
        </receiver>
        <!-- D.4 step 38: NotificationAlarmReceiver removed (account-expiry alarm dead). -->
        <receiver
                android:name=".receiver.AutoStartVpnBootCompletedReceiver"
                android:enabled="false"
                android:exported="false">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- D.4 step 38: ScheduleNotificationBootCompletedReceiver removed
             (was rescheduling account-expiry alarms after boot). -->

        <!--
         Receiver to warn Android 16 users about VPN upgrade issue
         https://issuetracker.google.com/issues/441315112
         -->
        <receiver
            android:name=".receiver.Android16UpdateWarningReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Serves the collected problem reports (and only them) to the app the
             user picks in the share sheet, one read grant per file. The
             authority follows the application id so the three product
             environments can be installed side by side. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.reports"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/report_paths" />
        </provider>
    </application>
</manifest>
