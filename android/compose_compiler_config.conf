// Classes the Compose compiler treats as stable although it cannot prove it:
// the model types composables take as parameters. They are value holders
// whose fields never change after construction (a read-only List, an
// InetAddress), so a composable taking one skips when it is unchanged instead
// of being restartable only. Every entry is a promise: keep these immutable.
//
// The compiler reads this file from the convention plugin
// (gradle/build-logic/src/main/kotlin/utilities/ComposeCompiler.kt); the
// stability reports it can emit (-Pwarren.app.build.compose.reports=true)
// list what is still unstable.
com.warrenbrowse.vpn.lib.model.TunnelState
com.warrenbrowse.vpn.lib.model.TunnelState.*
com.warrenbrowse.vpn.lib.model.GeoIpLocation
com.warrenbrowse.vpn.lib.model.InAppNotification
com.warrenbrowse.vpn.lib.model.InAppNotification.*
com.warrenbrowse.vpn.lib.model.ErrorState
com.warrenbrowse.vpn.lib.model.VersionInfo
com.warrenbrowse.vpn.lib.model.TunnelEndpoint
com.warrenbrowse.vpn.lib.model.FeatureIndicator
java.net.InetAddress
java.net.Inet4Address
java.net.Inet6Address
